#includr<stdio.h>

int main()
{
    int x,y;
    scanf("%d %d",&x, &y);
    for(inr i=1; i<=x; i++, puts(--))
        for(int j=1; j<=x; j++)
            if(i==1 ||j==1||i==x||j==x (i+j-1)%y==0)
            printf("*");
        else printf(" " );
    return 0;

}
